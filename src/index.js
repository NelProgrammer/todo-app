import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';

const htmlDocumentRoot = document.getElementById('root');
const root = ReactDOM.createRoot(htmlDocumentRoot);
root.render(<App />);
